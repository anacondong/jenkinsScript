@Library 'your-library-name' // If MyClass is in a shared library

node {
    stage('Build') {
        // Import the MyClass class
        def myClass = new MyClass()

        // Call the sayHello() method
        myClass.sayHello()
    }
}
